import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '18%',
  },
  containerTouchable: {
    borderWidth: 5,
    borderRadius: 40,
    width: 80,
    height: 80,
  },
  symbol: {
    fontSize: 48,
    textAlign: 'center',
  },
});
